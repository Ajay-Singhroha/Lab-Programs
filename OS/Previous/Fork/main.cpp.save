#include <iostream>
#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>
using namespace std;

int main()
{
    pid_t pid;
    pid=fork();
    if(pid<0)
    {
        fprintf(stderr,"Fork Failed ");
        return 0;

    }
    else if(pid==0)
    {
        printf("CHild Executing ");
    }
    else
    {
        w
        printf(" Child Completed ");
    }
    return 0;
}
